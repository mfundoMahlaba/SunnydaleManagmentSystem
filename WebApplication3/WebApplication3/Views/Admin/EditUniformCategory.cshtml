@model WebApplication3.Models.UniformCategory
@{
    ViewBag.Title = "Edit Uniform Category";
}

<h2>Edit Uniform Category</h2>

@using (Html.BeginForm("EditUniformCategory", "Admin", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)

    <div class="form-horizontal">
        <div class="form-group">
            @Html.LabelFor(m => m.Name, new { @class = "control-label col-md-2" })
            <div class="col-md-6">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Name)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-6">
                <input type="submit" value="Save Changes" class="btn btn-primary" />
                <a href="@Url.Action("UniformCategories", "Admin")" class="btn btn-secondary">Cancel</a>
            </div>
        </div>
    </div>
}
